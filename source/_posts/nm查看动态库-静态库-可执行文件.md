---
title: nm查看动态库/静态库/可执行文件
date: 2019-12-31 16:11:20
tags:
---
``` bash
nm:选项

-A 在每个符号信息的前面打印所在对象文件名称；
-C 输出demangle过了的符号名称；
-D 打印动态符号；
-l 使用对象文件中的调试信息打印出所在源文件及行号；
-n 按照地址/符号值来排序；
-u 打印出那些未定义的符号；

nm -A ./* | grep h264
nm -A *.a
nm -a *.so
nm -A a.out
```
